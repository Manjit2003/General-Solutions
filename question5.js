/**
 * Write a function that returns the elements on odd positions in a list.
 */

const myList = ["M", "A", "N", "J", "I", "T"];

for (var i = 1; i <= myList.length; i += 2) {
  console.log(myList[i]);
}
